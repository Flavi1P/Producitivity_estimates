# Figure Improvement Handoff — Implementation Tasks

**For:** the coding agent implementing `REVIEW.md`.
**Read first:** `REVIEW.md` (the reviewer's guidance) and `CLAUDE.md` (repo conventions,
scientific constants, how to run scripts). This document turns the review into discrete,
independently-testable tasks.

## Ground truth established during planning (don't re-derive)

- **NPP** `Output/cmems_npp_timeseries_domain_mean.csv` = **2015–2023**, monthly, columns
  `date, npp_int_mean, npp_int_std, npp_int_p10, npp_int_p90, n_cells`. Winter (Nov/Dec/Jan)
  rows exist but are **empty (`n_cells=0`, NaN)** — a genuine CbPM light gap, not a bug. The
  reprocessed CMEMS product stops 2023-12-27; extending to 2025 is upstream work (Task 8, optional).
- **NCP** `Output/IcelandIrminger_2015_2025/ncp/IcelandIrminger/ncp_uncertainty.xlsx` =
  **2015–2025**, **20-day bins**, columns `date_grid, NCP_mean, NCP_sd, NCP_q05, NCP_q95,
  n_iter, time_step_label`. Generated by an **external** `~/ncp_from_canyon/` toolbox — bin
  width and coverage **cannot** be changed from this repo.
- **GOP** input `Data/Processed/o2_budget_3902681_fullmld.csv`, columns include
  `type` (`net`/`night`), `mtime`, `z_bot_m`, `rate_corr_mmol_o2_m2_d`. 18-day
  centered median/IQR smoothing is applied **in the Python figures** (`WINDOW_DAYS = 18`),
  duplicated across `synthesis_gop_npp_ncp_figure.py`, `gop_vs_ncp_figure.py`,
  `gop_vs_ncp_fullmld_figure.py`, `gop_vs_ncp_fullmld_volumetric_figure.py`,
  `gop_vs_ncp_movingmld_figure.py`, `gop_depth_sensitivity_figure.py`. No shared config.
- Fig 2 also uses `Data/Processed/icb_npp_2025.csv` (float-track NPP+NCP, 2024-10→2025-11)
  and `Data/Processed/ncp_float_3902681_30d.xlsx`.
- The NPP figure's "winter = 0" comes from `_augment_winter_anchors` in
  `npp_timeseries_figure.py`, **not** from the data.

## Decisions already made (defaults; confirm with the PI if unsure)

1. **Coverage:** restrict basin comparisons/e-ratio to the **common 2015–2023** window
   (figure-level; no data downloads).
2. **NPP winter:** render as **"not estimated"** (drop hard-zero anchors); annual totals over
   the observed season only, with an explicit caveat.
3. **GOP winter mask:** **data-driven deep-mixing** — invalid where `z_bot_m` exceeds a
   deep-convection threshold (start at **200 m**, keep configurable).
4. **Smoothing:** shared config for the window; keep native product cadences but verify Fig 2
   ordering/lag survives equal smoothing and state cadences in captions.

## How to run (from repo root; see CLAUDE.md)

```powershell
& "C:/Users/flapet/AppData/Local/miniforge3/envs/productivity_py/python.exe" Scripts/<fig>.py
```

Figures are verified by inspecting the output PNG/PDF against `REVIEW.md` — there is no test suite.

---

# Tasks

Do **Task 1 first** (everything else imports it). Tasks 2–7 are then independent and can be
done in any order. Task 8 is optional/upstream.

---

## Task 1 — Shared figure config module  ⟶ enables all others   ✅ DONE

> **Status:** implemented in `Scripts/figure_config.py` (2026-07-02). Verified to
> import cleanly in the `productivity_py` env with no behavioural change to existing
> figures (module has **no matplotlib side effects** — it does not touch `rcParams`,
> so each figure keeps its own styling). Nothing else has been wired to it yet;
> Tasks 2–7 still need to replace their local duplicates with imports.
>
> **What was implemented:**
> - Constants: `WINDOW_DAYS = 18`, `MIN_N = 3`, `COMMON_YEAR_START = 2015`,
>   `COMMON_YEAR_END = 2023`, `GOP_DEEP_MIX_THRESHOLD_M = 200.0`.
> - Colours (with the alias names each source file already uses):
>   `GOP_COLOR`/`GOP_LIGHT`, `NCP_COLOR`==`BLUE`/`BLUE_LIGHT`,
>   `NPP_COLOR`==`GREEN`/`GREEN_LIGHT`, `ERATIO_COLOR`, `ZERO_LINE`, `MLD_COLOR`,
>   `MASK_BAND` (grey band for invalid spans), `P1_SHADE`/`P2_SHADE`.
> - Helpers: `centered_window_stats(times, values, grid, window_days=WINDOW_DAYS,
>   min_n=MIN_N)` (the one shared smoother — `min_n` is now a parameter, previously a
>   global); `render_masked(ax, x, valid_mask, y=None, ...)` (grey `axvspan` over each
>   contiguous invalid span + NaN-masks the line there; returns the masked `y`);
>   `assert_within_axis(ax, *arrays)` (raises `ValueError` if any finite plotted value
>   exceeds `ax.get_ylim()`; handles inverted depth axes).
>
> **Note for downstream tasks:** NPP green differed between files (`#2e7d32` in the
> synthesis figure vs `#1a6e3c` in `npp_timeseries_figure.py`). The config
> standardises on **`#1a6e3c`** (the file that defines the matched `GREEN`/`GREEN_LIGHT`
> pair), so when Task 3 imports from config the synthesis NPP line shifts very slightly
> darker — intended by this consolidation.

**Create** `Scripts/figure_config.py` as the single source of truth. It must expose:

- Smoothing: `WINDOW_DAYS = 18`, `MIN_N = 3`.
- Common years: `COMMON_YEAR_START = 2015`, `COMMON_YEAR_END = 2023`.
- Colours currently redefined per file: `GOP_COLOR`, `GOP_LIGHT`, `NCP_COLOR`/`BLUE`,
  `BLUE_LIGHT`, `NPP_COLOR`/`GREEN`, `GREEN_LIGHT`, `ZERO_LINE`, shading colours, `ERATIO_COLOR`.
- `GOP_DEEP_MIX_THRESHOLD_M = 200.0`.
- Helpers:
  - `centered_window_stats(times, values, grid, window_days, min_n)` — move the identical copy
    out of `synthesis_gop_npp_ncp_figure.py` / `gop_vs_ncp_*` so there is one implementation.
  - `render_masked(ax, x, valid_mask, ...)` — draws a grey band over invalid spans + suppresses
    the line there, so every figure renders masked periods identically.
  - `assert_within_axis(ax, *arrays)` — raises if any finite plotted value falls outside the
    current `ax.get_ylim()` (used by Task 3/5/6 to forbid silent clipping).

**Acceptance:** module imports cleanly in the productivity_py env; no behavioural change yet.

---

## Task 2 — Fig 1 trajectory (`Scripts/float_3902681_trajectory.py`)  ✅ DONE

> **Status:** implemented in `Scripts/float_3902681_trajectory.py` (2026-07-02).
> Re-run and inspected; output at `Output/float_3902681_trajectory.png`.
>
> **What was implemented:**
> 1. **Neutral markers** — deploy star and last-profile square are now **white fill /
>    black edge** (`c="white", edgecolor="k"`); shape alone distinguishes them, so
>    neither implies a date on the viridis colourbar.
> 2. **Leader-line labels** — the "deploy …" / "last …" labels are offset into open
>    water in small white boxes, each connected to its marker by a thin leader line
>    (`annotate(..., arrowprops=dict(arrowstyle="-", ...))`), off the Iceland-shelf tangle.
> 3. **Panel C redrawn** — replaced the dawn→dusk→dawn sequence with **dusk (18 h) →
>    pre-dawn (29 h, ~11 h later) → next dusk (42 h)**, matching the `night_tbl`
>    definition (dusk→dawn pairs < 0.7 d) in `o2_budget_3902681_fullmld.R`. The
>    dusk→pre-dawn night is highlighted with a darker band, a `<->` "night bracket ≈ 11 h"
>    span bar, and a "ΔO₂ = night respiration" annotation; title is now
>    "Night O₂-loss bracket (dusk → pre-dawn)".
>
> **Also changed (beyond the original task):**
> - **Panel B removed** at the PI's request (the local-solar-time zoom was deemed
>   redundant). Layout is now a single-row 2-panel gridspec (A map + C schematic),
>   `figsize` reduced to 14×5.8. `cdawn`/`cdusk` are still used by Panel C markers.
> - Fixed the stale run-command in the docstring.
>
> **Env note:** this script imports **cartopy**, which is NOT in `productivity_py`.
> It was run from the **`virtualfleet`** conda env:
> `& "C:/Users/flapet/AppData/Local/miniforge3/envs/virtualfleet/python.exe" Scripts/float_3902681_trajectory.py`.
> If Fig 1 should run alongside the other Python figures, add cartopy to `productivity_py`.

1. **Marker/colourbar mismatch:** the deploy star (`#1a9850` green) and last-profile square
   (`#d73027` red) clash with the viridis date colourbar (Oct 2024 ≈ dark purple). Recolour
   both start/end markers to a **neutral** style (e.g. white/grey fill, black edge) — or to the
   exact viridis endpoints — so no date is implied by the fill.
2. **Overlapping annotations:** the "deploy … / last …" labels sit on the dense Iceland-shelf
   tangle. Offset them into open water with **leader lines** (`annotate(..., arrowprops=...)`).
3. **Panel C schematic (`axs`):** currently draws dawn→dusk→dawn implying ~30 h dawn-to-dusk
   spacing, which reads as inconsistent with night-bracketing. Redraw so the **dusk→pre-dawn
   pair (~11 h apart)** that isolates nighttime O₂ loss is the obvious unit — matching the real
   `night` definition (dusk→dawn pairs < 0.7 d) in `o2_budget_3902681_fullmld.R`.

**Acceptance:** markers no longer imply a colourbar date; labels don't overlap the track;
schematic's highlighted unit is the dusk→pre-dawn night bracket.

---

## Task 3 — Fig 2 synthesis (`Scripts/synthesis_gop_npp_ncp_figure.py`) — most work  ✅ DONE

> **Status:** implemented in `Scripts/synthesis_gop_npp_ncp_figure.py` (2026-07-02).
> Re-run in `productivity_py`; output at
> `Output/synthesis_GOP_NPP_NCP_areal_volumetric.png` / `.pdf`.
>
> **What was implemented (per sub-item):**
> 1. Imports `WINDOW_DAYS`, `MIN_N`, `GOP_DEEP_MIX_THRESHOLD_M`, the colours,
>    `centered_window_stats`, `render_masked`, `assert_within_axis` from
>    `figure_config`; local duplicates deleted. NPP green therefore shifts to the
>    config `#1a6e3c` (the intended Task 1 consolidation).
> 2. **Winter GOP mask** = `_window_deep_mix_mask`: window-**median** z_bot >
>    threshold, then **eroded** by the smoothing half-window, then short valid
>    runs (< 18 d) dropped (also re-applied after clipping to the plot window so
>    no orphan stub shows). Net effect: GOP masked ~Nov 2024→Apr 23 2025; the
>    spring-onset (P1) GOP survives from Apr 23. Chosen over the simpler
>    window-**max** rule (which would have masked all of P1) because window-max
>    hid the spring-onset narrative; median+erosion is equally free of
>    deep-mixing contamination. `render_masked` draws the grey band; the raw
>    per-pair trace is suppressed throughout the masked span (verified: raw shown
>    only Apr 24–Oct 30).
> 3. **Band tightened**: the 18-day IQR fill is replaced by a robust
>    (MAD-based) **standard error of the median**, combined in quadrature across
>    the net + night medians, drawn at **±1 s.e.** (`SE_MULT`). The raw dusk/dawn
>    O₂ rates are genuinely very noisy, so a 95% bootstrap/MAD CI still saturated
>    the panel (and the bootstrap had −800…−940 tails); ±1 s.e. is ~3× tighter
>    than the IQR and readable in P1/P2 (it stays wide only in the noisy
>    autumn shoulder, which is honest). Legend label updated accordingly.
> 4. **No clipping**: `.clip(lo,hi)` calls removed; out-of-panel raw points are
>    dropped to NaN (not pinned to the border); `assert_within_axis` is called
>    after each panel on the data **lines** (GOP mean, NPP, NCP line + points).
>    The s.e. **envelope** is intentionally excluded from the assert — matplotlib
>    clips the shaded patch to the axes box, which is not silent line-clipping.
>    Panel y-limits (areal −140…300, vol −3…6.5) contain all masked data lines.
> 5. **Ceiling claim** reworded to "In the productive season, GOP sets the
>    gross-production ceiling…"; the P1 arrow now reads "spring onset: GOP, NPP,
>    NCP rise together as mixing ceases" (accurate now GOP is masked through
>    early P1).
> 6. **Legend** on a **solid white** box (`framealpha=1.0`, `zorder=20`), with an
>    added grey-patch entry for the deep-mixing mask.
> 7. **Caption footnote** (`fig.text`) states native cadences (GOP 18-day, NPP
>    ~10-day, NCP 30-day) and that the GOP≥NPP≥NCP ordering / P1→P2 lag were
>    checked under equal 18-day smoothing.
>
> **Tunables added:** `SE_MULT = 1.0` (band width in s.e.); mask knobs live in
> `_window_deep_mix_mask` (`min_run_days`, threshold from config).
>
> **Note:** `fig.tight_layout` emits a benign "Axes not compatible" warning
> (annotations/legend); layout is correct and `savefig` uses `bbox_inches="tight"`.

1. **Import from `figure_config`** (`WINDOW_DAYS`, `MIN_N`, colours, `centered_window_stats`,
   helpers) — delete the local duplicates.
2. **Winter GOP mask (deep-mixing):** compute a `valid` mask on the GOP series where
   `z_bot_m <= GOP_DEEP_MIX_THRESHOLD_M`; render invalid spans via `render_masked` (grey band,
   no line). Remove the faint raw per-pair trace in masked spans too.
3. **Tighten the uncertainty band:** replace the 18-day **IQR** fill (saturates the panel) with
   a **bootstrap CI of the median** (or median ± MAD). Update the legend label accordingly.
4. **Fix axis clipping:** set y-limits that contain the (masked) data; remove the `.clip(lo,hi)`
   calls in `draw_panel`; call `assert_within_axis` after plotting each panel.
5. **Ceiling claim:** restrict the "GOP sets the gross-production ceiling" annotation and the
   caption wording to the **productive season** — drop any "at all times of year" phrasing.
6. **Legend** to a clear region or onto a solid white box.
7. **Caption:** state native cadences (GOP 18-day, NPP ~10-day, NCP 30-day float budget) and
   note the ordering/lag was checked under equal smoothing.

**Acceptance:** winter GOP no longer dominates or dips below NPP/NCP as a raw spike; band is
readable; nothing exceeds y-limits (`assert_within_axis` passes); legend legible.

---

## Task 4 — Fig 3 NCP (`Scripts/ncp_timeseries_figure.py`) — minor

1. **Panel (b) top clipping:** grey individual-year splines (spring peaks ~55 and a Dec
   excursion) run past the axis. Raise `ylim` to contain them; assert (use Task 1 helper).
2. **Panel (a) marker density:** thin the markers (e.g. plot every Nth) or go line-only.
3. Confirm the **20-day** bin label is correct and consistent with the standardized NCP cadence.
4. Import colours from `figure_config` for consistency.

**Acceptance:** no clipped year lines; panel (a) less ink-dense; labels consistent.

---

## Task 5 — Fig 4 NPP (`Scripts/npp_timeseries_figure.py`)

1. **Remove hard-zero winter treatment:** delete `_augment_winter_anchors` zero anchors and the
   `[0.0] + ... + [0.0]` anchoring in `build_synthetic_year` and `individual_year_spline`.
   Render winter (Nov–Jan / NaN months) as **"not estimated"** using `render_masked` — grey band
   + no line — matching the Fig 2 mask style. The spline should span only observed months.
2. **Annual total:** if the figure/caption quotes an annual NPP (~225 g C m⁻² yr⁻¹), recompute
   over the **observed season only** and document the winter treatment in the caption.
3. **Coverage label:** reconcile with NCP per Task 7 (state 2015–2023 for NPP).
4. Keep `ylim` containing the data; assert.

**Acceptance:** no winter line pinned to 0; winter clearly shown as not-estimated; annual total
(if shown) documented; nothing clipped.

---

## Task 6 — Fig 5 e-ratio (`Scripts/eratio_synthetic_year_figure.py`) — the money figure

1. **Common years:** filter the NCP record to `<= COMMON_YEAR_END` **before** calling
   `ncp_mod.build_synthetic_year`, so NCP and NPP climatologies use the same 2015–2023 set.
   Re-quote any e-ratio number that changes.
2. **Dual-axis:** factor the left↔right transform into **one documented function** (e.g.
   `prod_to_eratio_axis()` / its inverse). Draw e-ratio gridlines/ticks **only on the right
   axis**; **remove the second (production=0) dotted line** or colour-code each reference line to
   its own axis so the two zero lines can't be confused.
3. **Mask:** reuse the shared `valid_season` (Feb–Oct / observed-NPP) object from Task 1 instead
   of the local `OBS_MONTHS`.
4. Import shared colours.

**Acceptance:** single unambiguous zero-reference per axis; axis mapping is one documented
function; e-ratio computed on common 2015–2023 years; caption number updated.

---

## Task 7 — Coverage-label consistency sweep (cross-cutting)

After Tasks 3–6, grep the figure scripts for hard-coded year strings ("2015–2025",
"2015–2023", "9 yr", "11 yr", "n = … yr") and make every caption/label state the **actual**
window used in that panel. Basin comparison figures and the e-ratio use **2015–2023**; the
single-float Fig 2 uses its own Nov 2024–Nov 2025 deployment window (unaffected).

**Acceptance:** no mismatched coverage labels remain across Figs 2–5.

---

## Task 8 — (Optional, upstream) Extend NPP to 2025

Only if the PI chooses to extend NPP rather than restrict to common years. Download the CMEMS
**NRT** chl/POC 0.25° product for 2024–2025, then:

```powershell
& "…/productivity_py/python.exe" Scripts/cmems_3d_cbpm.py --input <nrt.nc> --start 2024-01-01 --end 2025-12-31
& "…/productivity_py/python.exe" Scripts/cmems_npp_timeseries.py
```

Then rebuild Figs 4–5 on 2015–2025 and update `COMMON_YEAR_END`. Requires Copernicus/CDS
credentials (`~/.cdsapirc`) — do not automate credential provisioning.

---

## Final verification (all tasks)

Run each of the five figure scripts from repo root and inspect outputs against `REVIEW.md`'s
"Checklist for the coding agent". Confirm:

- [x] `figure_config.py` exists as the single source of truth (Task 1 done). Still
      TODO: make it the *only* place — Tasks 2–7 must delete their local duplicates.
- [x] Fig 1: neutral markers, non-overlapping annotations, dusk→pre-dawn schematic. (Task 2 done)
- [x] Fig 2: winter GOP masked, band tightened, no clipping, legend legible. (Task 3 done)
- [ ] Fig 3: panel (b) no top clip.
- [ ] Fig 4: no hard-zero winter; "not estimated" rendering; documented annual total.
- [ ] Fig 5: single zero per axis, documented mapping function, common-year e-ratio.
- [ ] Coverage labels consistent everywhere.
